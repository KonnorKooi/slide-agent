# ============================================================================
# MASTRA AI AGENT SERVICE - Environment Variables
# ============================================================================
# This file contains environment variables for the Mastra AI agent service
# that generates speaker notes for presentations.
#
# SETUP:
# 1. Copy this file to `.env` in the same directory
# 2. Fill in the actual values (never commit .env to git)
# ============================================================================

# ----------------------------------------------------------------------------
# OpenAI API Configuration
# ----------------------------------------------------------------------------
# Purpose: Used by SlideAgent (GPT-4o-mini) for generating speaker notes
# Get key from: https://platform.openai.com/api-keys
# Format: sk-proj-...
OPENAI_API_KEY=sk-proj-your-openai-api-key-here

# ----------------------------------------------------------------------------
# Google OAuth Credentials
# ----------------------------------------------------------------------------
# Purpose: Used by backend-auth.ts to create authenticated Google Slides API clients
# Get credentials from: Google Cloud Console > APIs & Services > Credentials
# Required APIs: Google Slides API must be enabled in your GCP project
#
# GOOGLE_CLIENT_ID: OAuth 2.0 Client ID (ends with .apps.googleusercontent.com)
# GOOGLE_CLIENT_SECRET: OAuth 2.0 Client Secret (starts with GOCSPX-)
# GOOGLE_REDIRECT_URI: OAuth redirect URI (use 'postmessage' for server-side flow)
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-your-client-secret
GOOGLE_REDIRECT_URI=postmessage

# ----------------------------------------------------------------------------
# Firebase Backend Connection
# ----------------------------------------------------------------------------
# Purpose: URL to Firebase Functions backend for fetching user OAuth tokens
# Development: Use local Firebase emulator
# Production: Use deployed Firebase Functions URL
#
# Format (dev): http://localhost:5001/{project-id}/{region}
# Format (prod): https://{region}-{project-id}.cloudfunctions.net
FIREBASE_FUNCTIONS_URL=http://localhost:5001/verbaslide/us-central1
# Production (uncomment when deploying):
# FIREBASE_FUNCTIONS_URL=https://us-central1-verbaslide.cloudfunctions.net

# ----------------------------------------------------------------------------
# Server-to-Server Authentication
# ----------------------------------------------------------------------------
# Purpose: API key for Mastra service to authenticate with Firebase backend
# Used by: backend-auth.ts when calling refreshOAuthToken Cloud Function
# Security: Must match SERVER_API_KEY in backend/.env (keep this secret!)
#
# Generate a secure key with: openssl rand -hex 32
SERVER_API_KEY=your-secret-server-api-key-here

# ----------------------------------------------------------------------------
# Optional: API Server Port
# ----------------------------------------------------------------------------
# Purpose: Port for the Express API server (api-server.js)
# Default: 3001
# API_PORT=3001
