# OpenAI API Configuration
OPENAI_API_KEY=sk-proj-your-openai-api-key-here

# Google Slides OAuth Configuration
# Place your OAuth 2.0 credentials file as 'credentials.json' in the project root
# Get credentials from: Google Cloud Console > APIs & Services > Credentials
# Enable the Google Slides API in your Google Cloud project
# The getSlide tool will store authentication tokens in 'token.json' automatically